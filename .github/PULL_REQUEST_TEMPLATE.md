## What this changes

## How it was tested

- [ ] `pytest -q` passes locally
- [ ] `ruff check .` is clean
- [ ] `fruitfly-motor check` passes
- [ ] `python examples/sim_demo.py` runs

## Notes

Anything that touches the shaper order, the wire format or a stop rule needs a line
in `docs/VALIDATION.md` explaining what the numbers did.
